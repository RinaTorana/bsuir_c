#include <stdio.h>
#include <conio.h>
#include <math.h>
#include <locale.h>
#include <process.h>

/*
void main() {

	double x, y, z, a, b, c;

	start: // tag used for goto operator in case same x and y are entered

	printf("Enter x \n");
	scanf_s("%lf", &x);

	printf("Enter y \n");
	scanf_s("%lf", &y);

	if (x == y) {

		printf("X equals y. Please enter not equal values \n");
		goto start;	
	} 

	if (x < 0) {
		a = x;
	} else {
		a = 0;
	}

	if (y < 0) {
		b = y;
	} else {
		b = 0;
	}

	if (y > x) {
		c = pow(y, 2);
	} else {
		c = pow(x, 2);
	}

	if (c == 0) {
		printf("It is not possible to divide to 0 \n");
	} else {

		z = (a - b) / c;
		printf("z=%.1lf \n", z);
	}

	_getch();

}
*/