#include <stdio.h>
#include <conio.h>
#include <math.h>
#include <locale.h>
#include <process.h>
#define Pi 3.1415926
/*
void main() {

	setlocale(LC_CTYPE, "Russian");
	double a, x, y, z;

	printf("Enter x:");
	scanf_s("%lf", &x);
	printf("Enter y:");
	scanf_s("%lf", &y);
	printf("Enter z:");
	scanf_s("%lf", &z);

	a = pow(2, -x) * sqrt(x + pow(fabs(y), 1. / 4))*(pow(exp(x - 1. / sin(z)), 1. / 3));

	printf("\n Results of the calculations: a=%.5lf \n", a);
	_getch();

}
*/